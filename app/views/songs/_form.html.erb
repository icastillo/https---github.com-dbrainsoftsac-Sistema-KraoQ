<%= simple_form_for(@song) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
 

	<%= f.collection_select :artist_id, Artist.order(:no_artista), :id, :no_artista, {prompt: "Selecione un artista"}, {class: "form-control"} %>


    <%= f.input :no_cancion %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
